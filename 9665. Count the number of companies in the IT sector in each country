'''
Count the number of companies in the Information Technology sector in each country.
Output the result along with the corresponding country name.
Order the result based on the number of companies in the descending order.
'''
# Import your libraries
import pandas as pd

# Start writing code
forbes_global_2010_2014.head()
df = forbes_global_2010_2014[forbes_global_2010_2014['sector'] == 'Information Technology']
it_country = df.groupby('country')['company'].count().reset_index()
it_country.sort_values('company', ascending = False)
