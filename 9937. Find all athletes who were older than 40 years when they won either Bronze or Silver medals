# Import your libraries
import pandas as pd

# Start writing code
olympics_athletes_events.head()
olympics_athletes_events[(olympics_athletes_events['age'] > 40) & (olympics_athletes_events['medal'].isin(['Bronze','Silver']) )]
